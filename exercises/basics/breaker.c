#include <stdio.h>

int main(int argv, char** argc) {
	for (int i = 0; i <= 10; ++i) {
		printf("write 'i will not include obscentities in my public repos' on the whiteboard %d times", i);	
	}
	return 0;
}

